const initialState = []

export const todoItemReducer = (state = initialState, action) => {
    switch(action.type) {
        case "add": 
        break
        case "edit":
        break;
        case "remove": 
        break
        default
        break
    }
}